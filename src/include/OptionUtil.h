#ifndef _OPTION_UTIL_H_
#define _OPTION_UTIL_H_
#include <string>
using namespace std;
bool SaveOption(char * pConfig);
string ParseOption(char * pConfigFile);
bool SaveUserChoice(char * pConfig);
#endif
